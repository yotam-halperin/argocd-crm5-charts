apiVersion: v1
kind: ConfigMap
metadata:
  name: myconf
  namespace: {{ .Values.namespace }}
data:
  MONGODB_URI: "mongodb://{{ .Values.mongodb.auth.rootUser }}:{{ .Values.mongodb.auth.rootPassword }}@yotam-crm4-mongodb-headless"
  LOG_LEVEL: "info"
  PERSISTENCE: "true"